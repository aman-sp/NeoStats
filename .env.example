# =============================================================
# NeoStats Credit Risk Intelligence Platform — Environment Config
# =============================================================

# Optional: External LLM API Keys for Generative NL-to-SQL
# If omitted, the platform operates seamlessly using its built-in verified deterministic SQL engine.
GEMINI_API_KEY=
OPENAI_API_KEY=

# Platform Port (Default: 8501)
STREAMLIT_SERVER_PORT=8501
STREAMLIT_SERVER_ADDRESS=0.0.0.0
