# Environment and Credentials
.env
.env.local
*.env

# Python Cache & Bytecode
__pycache__/
*.py[cod]
*$py.class
*.so
.pytest_cache/

# Virtual Environments
venv/
env/
.venv/
ENV/

# Home Credit Raw Datasets (Do NOT commit to git per assignment rules)
HC_*.csv
HomeCredit_columns_description.csv
application_train*.csv
*.zip

# Database warehouse is required for runtime validation and clean-clone execution.
# Keep the SQLite warehouse tracked so deployment and local reproduction do not depend on a missing local artifact.
!database/
!database/credit_risk_warehouse.db

# IDE & OS
.vscode/
.idea/
.DS_Store
Thumbs.db
